parameters:
- name: booleanParameter # name of the parameter; required
  type: boolean # data type of the parameter; required
  default: false

steps:
- pwsh: echo "${{ parameters.booleanParameter }}"